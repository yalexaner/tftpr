[package]
name = "tftpr"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["net", "rt-multi-thread", "macros", "io-util", "fs", "time"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
